# 조건에 따라 프로그램의 동작을 바꿀 수 있는 기능
# 조건부 명령문(조건문)이 위와 같은 역할을 수행

if x > 0
  println("x is positive")
end

# if 다음에 나오는 논리 표현식을 조건이라 명칭
# 만일 조건이 참이면, 들여쓰기 된 문장 실행, 참이 아니면 실행 X

# if 명령문은 함수 정의와 구조가 동일(헤더가 있고, end로 끝나는 본문)

if x < 0
  # TODO: 음수인 경우를 처리해야 함!
end  
