# 변수 갱신 (variable update)

# 변수의 기존 값을 가지고 새로운 값을 만드는 것

x = x + 1    # x의 현재 값을 가져와 1을 더해 새로운 값을 만든 후, 다시 x가 그 값이 되도록 갱신

# 존재하지 않는 변수에 갱신을 시도하면 오류 발생. 변수에 값을 할당하기 전에 표현식을 먼저 평가

# 변수를 갱신하기 전에는 먼저 초기화를 해야합니다. 간단한 할당으로 진행됩니다.

y = 0

y = y + 1
