# 합성 (compose)

# 코드들의 작은 부분을 모아서 합성도 가능

x = sin(degrees / 360 * 2 * 𝛑)     # 함수의 인수는 산술연산을 포함해 어떤 표현식이어도 됨  

x = exp(log(x+1))                  # 인수로 전달하는 표현식이 함수 호출이어되 됨.

minutes = hours * 60               # 일반적으로 할당문의 왼편에는 변수명이 아닌 다른 표현식이 들어가면 안됨. 
#120
